<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Access files in the external storage -->
    <external-path name="external_files" path="." />

    <!-- Access files in the app's cache directory -->
    <cache-path name="cache_files" path="." />

    <!-- Access files in the app's internal files directory -->
    <files-path name="internal_files" path="." />

    <!-- Access files in the app's external media directory -->
    <external-files-path name="external_media_files" path="." />
</paths>